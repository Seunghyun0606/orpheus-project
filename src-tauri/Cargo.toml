[package]
name = "orpheus-desktop"
version = "0.1.0"
description = "Orpheus desktop shell"
authors = ["Vantage Systems"]
edition = "2021"

[lib]
name = "orpheus_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
